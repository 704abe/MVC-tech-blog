<div class="text-center">
  <h2>{{title}}</h2>
  <p>{{content}}</p>
  <p>Created by {{user.username}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{title}}</span>
  </p>
  <p>
    <button class="btn btn-primary">Leave a comment!</button>
  </p>
</div>
